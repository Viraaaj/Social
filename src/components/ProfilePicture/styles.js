import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainView: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#b366b3',
  },

  image: {
    borderRadius: 40,
    borderWidth: 1,
    borderColor: '#ffffff',
  },
});

export default styles;
