import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainView: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#b366b3',
    height: 66,
    width: 66,
  },

  image: {
    height: 60,
    width: 60,
    borderRadius: 40,
    borderWidth: 1,
    borderColor: '#ffffff',
  },
});

export default styles;
